PORT=8006
DEPARTMENT_NAME="Department of Records & Compliance"
DEPARTMENT_CODE="REC"
KB_NAMESPACE="records-compliance"
WELCOME_MESSAGE="Hello! I am the Records & FOIA Assistant. I can assist you with submitting public records requests, checking document disclosure status, and retrieving county records."
SUPPORT_EMAIL_CONTACT="records@riversidecounty.gov"
OFFICE_HOURS_INFO="Weekdays 9:00 AM - 3:00 PM"
INQUIRY_AGENT_URL="http://localhost:8001/chat"

# Remote MCP Server Endpoints (SSE). Point these at wherever the MCP servers
# are actually deployed (e.g. an Agent Manager MCP proxy in front of each) —
# defaults assume each is running locally with MCP_TRANSPORT=sse for testing.
KB_MCP_URL="http://localhost:9001/sse"
RECORDS_DB_MCP_URL="http://localhost:9007/sse"

LLM_PROVIDER_KEY="azure-openai-gov"
LLM_MODEL_NAME="gpt-4o"
MODEL_TEMPERATURE="0.1"
MAX_TOKENS="1000"
