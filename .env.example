# Infrastructure & Shared API Keys
# OPENAI_API_KEY is used both by every agent's LLM and by the local-kb MCP
# server (mcp_servers/local_kb_mcp) to embed the knowledge base for local RAG
# search -- no external vector DB is used, see mcp_servers/local_kb_mcp/kb_index.py.
OPENAI_API_KEY="your_openai_api_key_here"
