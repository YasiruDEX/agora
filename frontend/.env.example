# When true (default), the chat UI uses realistic pre-scripted mock agent
# responses (src/mock/agentMocks.js) — no backend agents required.
# Set to false to send real POST /chat requests to the standalone agent
# servers on ports 8000-8006 (see src/services/agentApi.js).
VITE_USE_MOCK_AGENTS=true
